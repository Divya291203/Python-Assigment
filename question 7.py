# TWrite a python program that takes a string as input and returns its length.

def string_length():
    user_input = input("Enter a string: ")
    print(f"The length of the string is {len(user_input)}")

string_length()
